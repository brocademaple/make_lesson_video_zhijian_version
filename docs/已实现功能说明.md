# 项目已实现功能说明

| 元数据 | 内容 |
|--------|------|
| **文档版本** | 2.4 |
| **说明对象** | 仓库路径 `text2classvideo` 当前已实现行为 |
| **修订沿革** | 见文末 **「文档修订记录」**；以其中 **最新日期、该日期下最上方一条** 为当前文档最近一次变更说明 |

### 源码版本对照（与安装包 / 代码保持一致）

| 名称 | 版本 | 依据文件 |
|------|------|----------|
| **ppt-course-mvp** / **课件处理与工作台**（Python 包 `ppt_course_deal`，命令 `ppt-course`） | **0.1.0** | 根目录 `pyproject.toml` 中 `[project].version`，以及 `ppt_course_deal/__init__.py` 中 `__version__` |
| **视频渲染（Remotion）**（`ppt_course_renderer/`，独立 Node 工程占位） | **未纳入根目录 PyPI 版本** | 无统一 `pyproject.toml` 版本；目录内 **`README.md`** 描述与 `ppt_course_deal` 的数据契约及 [Remotion](https://github.com/remotion-dev/remotion) 集成方向。 |
| **AI 课程重构管线** / **Rebuilder**（`ppt_course_rebuilder/`，独立 CLI） | **未单独发布 PyPI 版本号** | 目录内无与根目录统一的 `pyproject.toml` 版本字段；功能范围以该目录下 `main.py`、`README.md` 及本文对应章节为准。后续若在子项目中增加版本声明，以该声明为准。 |

本文档仅描述**上述版本源码中已落地**的能力；与「已完成功能」无关的内容，统一放在文末 **「待实现与未纳入范围」** 标题下，不与上文混写。

仓库主要包含 **`ppt-course-mvp`（包名 `ppt_course_deal`）**、**`ppt_course_renderer/`**（Remotion 成片，目录占位）与独立子项目 **`ppt_course_rebuilder/`** 三部分；当前主线为 **deal → renderer** 管线调试，Rebuilder 的 Agent 向扩展可暂缓。

### 模块如何区分（建议在 README / 口头对齐时用中文名）

| 中文定位 | 目录或包名 | 要点 |
|----------|------------|------|
| **课件处理与工作台** | 包 **`ppt_course_deal`**，命令 **`ppt-course`** | **默认主力**：Web + CLI；课程化流水线为 **启发式规则**（非 Rebuilder 的 LLM 全流程）；数据默认 **`ppt_course_data/`**。 |
| **视频渲染（Remotion）** | **`ppt_course_renderer/`** | 与 **`ppt_course_deal` 平行**：约定接收每页 PNG、TTS 音频、逐字稿，生成 Remotion 合成数据并导出成片；详见该目录 **README**。**具体 Node / Remotion 工程文件将在该目录内增量添加。** |
| **AI 课程重构管线（Rebuilder）** | **`ppt_course_rebuilder/`** | **可选、非当前主线**：独立 venv 与 `requirements.txt`；依赖 **OpenAI 兼容 API**；产出 **`output/`** 下 JSON、新版 PPTX、`page_scripts.md` 等。**与 deal / renderer Web 未自动串联**。 |

---

## 一、课件处理与工作台：`ppt_course_deal`（`ppt-course-mvp` 包）

面向：将文字偏多的 `.pptx` 转为更偏「录课稿」的版式；提供 **命令行** 与 **本机 Web** 两种使用方式。

### 1.1 命令行

| 能力 | 说明 |
|------|------|
| `ppt-course transform` | 输入一份 `.pptx`，输出同目录下 `原文件名_course.pptx`（可用 `-o` 指定输出路径） |
| `ppt-course transform --plan-json` | 可选：将规划结果写出为 JSON 文件，便于排错或对接外部流程 |
| `ppt-course serve` | 启动 FastAPI + 静态前端（默认 `127.0.0.1:8765`，支持 `--host`、`--port`、`--reload`） |
| `ppt-course --version` | 打印包版本号 |

入口：`python -m ppt_course_deal` 与 `ppt-course` 等价。

### 1.2 核心流水线（与 CLI / Web 共用）

| 环节 | 已实现行为 |
|------|------------|
| 读取 | 使用 `python-pptx` 打开 `.pptx`，逐页遍历形状（含组合图形内的文本） |
| 结构化抽取 | **启发式**：按关键词与规则将文本归为规则 / 案例 / 处罚 / 互动 / 小结 / 叙述等类型（见 `BlockKind`），非 LLM |
| 页面规划 | 按固定区块顺序聚合，超长内容按字数拆成「续页」 |
| 生成新版式 | 空白版式 + 标题区 + 分区正文文本框；可为备注页写入原始页摘录（便于口播参考） |
| 输出 | 写入新的 `.pptx` 文件 |

### 1.3 Web 服务（`ppt_course_deal/web/app.py`）

依赖：`FastAPI`、`uvicorn`、`python-multipart`（随根目录 `pyproject.toml` 安装）。

#### HTTP 接口

| 路径 | 方法 | 说明 |
|------|------|------|
| `/` | GET | 返回单页工作台前端（`static/index.html`） |
| `/assets/*` | GET | 静态资源（CSS / JS） |
| `/api/health` | GET | 健康检查：``status``、``max_upload_mb``；另含 ``preview_render``（``ready``、``libreoffice`` / ``pdftoppm`` 是否探测到、可选路径、``install_hint_zh`` 安装提示） |
| `/api/parse` | POST | `multipart` 上传 `.pptx`，返回 JSON：文件名、页数、每页文本结构（标题、全文、`text_blocks`、备注、版式名、形状内图片数/表格数等）；可选生成预览会话；成功时在服务端**持久化**本次解析（见下），响应含可选字段 `task_id` |
| `/api/tasks` | GET | 已持久化解析任务的**摘要列表**（文件名、页数、创建时间、是否有预览图等） |
| `/api/tasks/{task_id}` | GET | 指定任务的完整解析 JSON（结构与解析接口正文一致，含 `slides` 全文） |
| `/api/tasks/{task_id}/preview/{slide_index}` | GET | 该任务已保存的第 `slide_index` 页 **PNG**（与临时会话 `/api/preview/...` 并存） |
| `/api/tasks/{task_id}` | DELETE | 删除该任务的本地目录（不可恢复） |
| `/api/preview/{session_id}/{slide_index}` | GET | 若预览会话存在且索引合法，返回对应页的 **PNG**（`image/png`） |
| `/api/transform` | POST | 上传 `.pptx`，返回课程化后的 **完整 `.pptx` 二进制**；响应头含 `X-Source-Slides`、`X-Output-Slides` |
| `/docs` | GET | FastAPI 自动生成的 OpenAPI 文档页面 |

上传体积上限：**默认单文件 50MB**；环境变量 **`PPT_COURSE_MAX_UPLOAD_MB`** 可提高到至多 **4096**（MB）。`/api/parse` 与 `/api/transform` 使用同一上限；服务端按 **分块**读入并在超过上限时拒绝，避免单次超大 `read()`。极大文件仍会整份载入内存做解析 / 转换，请保证机器内存与磁盘。若经 **Nginx / Caddy** 等反代，需同步放宽请求体大小（如 `client_max_body_size`）。命令行 **`ppt-course transform`** 无此 Web 上限。

#### 浏览器端界面（`ppt_course_deal/static/`）

| 能力 | 说明 |
|------|------|
| 上传 | 选择或拖放 `.pptx`（主页约为 **35% : 65%** 两栏：**左侧为已存任务列表**、右侧为上传区） |
| 已存任务 | 主页左侧栏列出本机已持久化的解析记录（可滚动）；点击条目在**主界面**进入工作台（左侧切换为**幻灯片缩略图** + 右侧大图，接口 `/api/tasks/...`）；支持刷新列表、单项删除；删除当前正在查看的任务会退回上传页 |
| 解析后布局 | 工作台为约 **35% : 65%** 两栏：左侧为**可纵向滚动**的整页缩略图列表（滚动条在列表区域右侧）；右侧为当前页大图与说明；其下 **「当前页解析文本」**为可折叠面板（**默认展开**）；再下为**音频工作台** |
| 整页缩略图 / 大图 | **上传会话**：缩略图与大图使用 `/api/preview/{session_id}/{index}`。**已存任务**：使用 `/api/tasks/{task_id}/preview/{index}`。未生成 PNG 时列表为数字占位，右侧仅文本 |
| 生成课程化 | 使用「生成课程化 PPTX」按钮，对**当前已上传文件**再次请求并下载（与命令行 `transform` 同一套流水线）；**仅从已存任务打开、未重新上传文件时**该按钮不可用 |
| 更换文件 | 回到上传区，可换另一份 `.pptx` 重新解析 |

#### 整页 PNG 预览（可选能力）

- **条件**：运行 Web 服务的机器已安装并可调用 **LibreOffice（`soffice`）** 与 **Poppler（`pdftoppm`）**。  
- **过程**：`PPTX → PDF（LibreOffice）→ 逐页 PNG（pdftoppm）`，文件落在临时目录；通过 `session_id` 绑定会话。  
- **会话**：内存中登记预览会话，临时目录超出数量上限（约 30 个会话规模）时淘汰较早会话并删除目录。  
- **限制**：刷新浏览器后需重新上传；**不是**公网 CDN，仅为同源临时 URL。

### 1.4 解析结果本地持久化（可选目录）

| 项 | 说明 |
|----|------|
| 触发时机 | 每次 `/api/parse` **解析成功**（含仅有文本、无 PNG 的情形）后写入磁盘 |
| 默认目录 | 仓库根目录 **`ppt_course_data/tasks/{uuid}/`**（内含 `meta.json`（解析全文）、`source.pptx`、`previews/slide-*.png`（若有））；旧版曾为 `ppt_course/user_data/` 或 `ppt_course_deal/user_data/`，若仍在使用环境变量 `PPT_COURSE_DATA` 则以该路径为准 |
| 环境变量 | `PPT_COURSE_DATA`：指定数据根目录（绝对或相对路径均可），任务位于其下 `tasks/` |
| 容量 | 任务数超过 **200** 条时自动删除**最旧**目录 |

### 1.5 配置与工程

| 项 | 说明 |
|----|------|
| Python 版本 | `>=3.9`（`pyproject.toml`） |
| 静态文件打包 | `ppt_course_deal/static/` 通过 `setuptools` `package-data` 打入 wheel |
| Web 上传上限 | **`PPT_COURSE_MAX_UPLOAD_MB`**：整数 MB，默认 **50**，上限 **4096**；前端启动时请求 `/api/health` 取得 `max_upload_mb` 用于上传前提示 |

---

## 二、视频渲染：`ppt_course_renderer/`（Remotion，目录占位）

与 **`ppt_course_deal`** **平行**，用于把 **逐页图片、MiniMax 等语音、逐字稿** 转为 **Remotion** 合成输入并最终成片。官方框架仓库：[remotion-dev/remotion](https://github.com/remotion-dev/remotion)；Cursor / Agent 侧编码规范可参考 [remotion-dev/skills](https://github.com/remotion-dev/skills) 中的 **`skills/remotion/`**。

| 项 | 当前状态 |
|----|----------|
| 目录与 README | 已建立 **`ppt_course_renderer/README.md`**，说明数据流与集成约定 |
| Node / Remotion 工程 | **尚未**在本仓库提交 `package.json` 与 Composition；后续在本目录内初始化（如 `npx create-video@latest`） |
| 与 `ppt_course_deal` 的契约 | **待定**（任务目录、`meta.json`、音频路径、字幕时间轴等由管线调试阶段固化） |

---

## 三、AI 课程重构管线：`ppt_course_rebuilder/`（独立 CLI 子项目）

与 **课件处理与工作台**（根目录包 `ppt_course_deal`）**并存**，单独目录、单独 `requirements.txt` 与 `main.py`，**不依赖** deal 的 Web；也**不是**「仅给调试用」。当前阶段以 **deal → renderer** 成片管线为主，**Rebuilder 的 Agent 版扩展可暂缓**。

### 3.1 运行形态

| 能力 | 说明 |
|------|------|
| `python main.py --mode analyze` | 读取 `--input` 指定 `.pptx`，写出 `output/slide_analysis.json` |
| `python main.py --mode plan` | 读取已有 `slide_analysis.json`，经 AI 规划后写出 `output/course_slides.json`；无密钥或失败时用本地兜底规划 |
| `python main.py --mode build` | 读取 `--course-slides`（默认 `output/course_slides.json`），生成 `--output` 指定 `.pptx` 与 `output/page_scripts.md` |
| `python main.py --mode full` | 串联 analyze → plan → build；可选 `--export-images` 尝试用本机 LibreOffice 导出 PNG 到 `output/exported_images/` |
| 环境变量 | `.env`：`AI_API_KEY`、`AI_BASE_URL`、`AI_MODEL`（见 `.env.example`） |
| 无 AI 时试跑模板 | 可使用 `output/sample_course_slides.json` 跑 `build`，验证版式与生成 |

### 3.2 已实现模块要点

| 模块 | 作用 |
|------|------|
| `src/ppt_reader.py` | 读取每页标题、文本块、图/表计数、备注、版式名等 |
| `src/models.py` | `RawSlide`、`SlideAnalysis`、`CourseSlide` 等 Pydantic 模型；课程页含 `source_slide_indexes`；预留 `image_prompt` / `image_url` / `asset_urls` 字段 |
| `src/ai_client.py` | OpenAI 兼容 Chat Completions，`response_format` JSON，失败重试一次 |
| `src/slide_analyzer.py` | 按 Prompt 输出单页 `SlideAnalysis`；失败时用本地兜底分析 |
| `src/course_planner.py` | 将多页分析合并为 `CourseSlide` 列表；失败时用保守「每页一张 rule_card」兜底 |
| `src/template_engine.py` + `templates/theme.py` | 按 `CourseSlide.type` 渲染 title / agenda / transition / rule_card / case_dialogue / quiz / explanation / summary；本地 `templates/assets/` 占位图标（缺失时可由代码生成） |
| `src/ppt_writer.py` | 汇总生成 `.pptx`，单页失败时插入错误提示页并继续 |
| `src/image_exporter.py` | 可选：检测 `soffice`/`libreoffice` 将 `.pptx` 转 PNG |
| `main.py` | 写出 `output/summary_report.json`；统一错误与警告收集 |

子项目自带 **`README.md`**（安装、命令、产物路径），与本文档互补。

---

## 四、待实现与未纳入当前交付的能力

以下内容**尚未在本仓库实现**，或仅预留字段 / 文档设想，**请勿与上文「已实现」混淆**。

| 项 | 说明 |
|----|------|
| 公网图床 / OSS | Web 预览仅为会话期临时目录 + 同源 URL，未对接对象存储或 CDN。 |
| Web 内编辑后再生成 | 浏览器中修改解析文本再驱动 `transform` 的流程未做。 |
| **`ppt_course_deal` Web 接入大模型** | 当前 Web 与 CLI 共用同一套**启发式**流水线，未在页面中接入 AI 分析 / 规划 API。 |
| **AI 课程重构管线** 的 Web UI | 该子项目按规格为 **CLI 优先**，未提供独立前端。 |
| 账号、权限、多租户 | 无。 |
| **deal → renderer 端到端成片** | `ppt_course_renderer/` 尚为目录占位；与 `ppt_course_deal` 的**稳定数据契约**、Remotion 工程、CI/本地 `npx remotion render` 联调等**未闭合**。 |
| 剪映等第三方剪辑自动化 | 未实现。 |
| 真实生图、外部 Skill 编排 | `CourseSlide` 虽预留图相关字段，无实际生图或图床写入逻辑。 |
| 动画与复杂母版深度还原 | 生成侧以简单空白版式 + 文本框为主，不保证还原原稿动画与品牌母版全部特性。 |

---

## 文档修订记录

### 编排规则（请先读）

- **日期是大分类**：每一档独立的日历日以三级标题 **`### YYYY-MM-DD（中文日期）`** 表示；**多个日期之间**：较新的日期写在**上面**，较早的日期写在**下面**。  
- **同一天内可能有多轮迭代**：在同一日期标题下，用四级标题 **`#### 迭代 n`** 区分（`n` 可取 1、2、3…，**数值越大表示当日越晚、越新**）；**同一天内**，较新的迭代写在**上面**，较早的迭代依次往下（即从**上到下 = 从新到旧**）。  
- 若当日最新一轮需要醒目，可在标题后缀 **`（本日最新）`**。

---

### 2026-05-07（2026年5月7日）

#### 迭代 14（本日最新）

- Web：「当前页解析文本」折叠区**默认改为展开**（进入工作台即可见抽取文本，仍可通过标题栏收起）；**文档版本** **2.4**。

#### 迭代 13

- Web：纠正布局语义——**未解析时**主页左侧为**已存任务**列表（非缩略图占位），右侧上传；**顶栏任务抽屉**与侧栏重复已移除；进入工作台后左侧为幻灯片缩略图；**文档版本** **2.3**。

#### 迭代 12

- Web：**未解析时的主页**亦采用约 **35% : 65%** 两栏（左侧区域原先用占位文案，后改为任务列表见迭代 13），右侧上传区；窄屏仍单列堆叠；**文档版本** **2.2**。

#### 迭代 11

- Web：已存任务**不再使用弹窗**；点击任务后在**主界面**以约 **35% : 65%** 展示**可滚动**缩略图列表 + 右侧大图；上传解析后的页切换亦改为**左侧列表**（不再使用下拉选择器）；**文档版本** **2.1**。

#### 迭代 10

- Web：预览区下方 **「当前页解析文本」**改为可折叠面板（默认收起，右侧 chevron），节省纵向空间、突出**音频工作台**；**文档版本** **2.0**。

#### 迭代 9

- Web 布局：**已存任务**迁入顶栏触发的**左侧抽屉**（默认收起）；上传页改为单列；解析完成后左侧仅保留**当前任务**的幻灯片页选择；**文档版本** **1.9**。

#### 迭代 8

- 整页预览：`slide_render` 在 macOS 上对 **`pdftoppm` 增加 Homebrew 常见路径探测**（缓解 IDE 启动服务时 PATH 不含 brew）；新增 **`describe_preview_render_env()`**；`/api/health` 增加 **`preview_render`**；上传区依赖未就绪时显示横幅；`ppt-course serve` 启动时黄色提示；README 增加「整页预览图」安装说明；**文档版本** **1.8**。

#### 迭代 7

- Web：上传体积上限改为环境变量 **`PPT_COURSE_MAX_UPLOAD_MB`**（默认 50MB，最高 4096MB）；`/api/health` 增加 **`max_upload_mb`**；解析与 transform 使用分块读入；前端拉取健康检查后按上限预检；README 补充说明；**文档版本** **1.7**。

#### 迭代 6

- Python 包 **`ppt_course` → `ppt_course_deal`**（导入路径与 `pyproject.toml` 脚本入口同步）；新增平行目录 **`ppt_course_renderer/`**（Remotion 成片，`README` 约定数据流；引用 [remotion-dev/remotion](https://github.com/remotion-dev/remotion)、[remotion-dev/skills](https://github.com/remotion-dev/skills)）；主线表述调整为 **deal → renderer**，Rebuilder Agent 向扩展**暂缓**；同步 README、`KNOWLEDGE`、`已实现功能说明`、Cursor skills；**文档版本** **1.6**。

#### 迭代 5

- 在 README、`docs/已实现功能说明.md`、`docs/KNOWLEDGE.md`、`ppt_course_rebuilder/README.md` 与 Cursor skill 中统一区分 **课件工作台（当时包名 `ppt_course`）** 与 **AI 课程重构管线（`ppt_course_rebuilder/`）**；**文档版本** **1.5**。

#### 迭代 4

- 持久化默认路径调整为仓库根 **`ppt_course_data/tasks/`**（仍可通过 `PPT_COURSE_DATA` 覆盖）；**文档版本** **1.4**。

#### 迭代 3

- Web：解析结果**本地持久化**；新增接口 `GET/DELETE /api/tasks/...`、预览图 `GET .../preview/{i}`；上传区展示**已存任务**列表，点击弹窗回顾解析；**文档版本**递增至 **1.3**。

#### 迭代 2

- 修订记录改为「**日期为大类**、日内多轮 **迭代从上到下为新→旧**」；文首元数据改为指向文末沿革，**文档版本**递增至 **1.2**。

#### 迭代 1

- 增加文档版本、修订日期及与源码 **0.1.0** 的对照表；明确 `ppt_course_rebuilder` 的版本表述方式（对应此前文档版本 **1.1** 的内容）。

---

### 文档维护约定

1. **追加修订时**：  
   - 若为新的一天：在上方**新建**一个 `### 日期` 块（保持「新日期在上」）。  
   - 若为同一天再改：在该日期块**最上方**新增一节 **`#### 迭代 n（本日最新）`**（`n` 为本日已有最大迭代号 + 1），并把上一轮的「（本日最新）」从旧条目中删掉，仅保留在最新一条上。  
2. 发布或变更 **`ppt-course-mvp` 版本号**（修改根目录 `pyproject.toml` / `ppt_course_deal/__init__.py`）时，应核对本文「已实现」章节是否与行为一致，并同步更新 **源码版本对照**；必要时在新一轮「修订记录」中写明。  
3. 可视需要递增文首 **文档版本**（如 1.2 → 1.3）；纯措辞微调也应记在当日某一迭代的条目内。  
4. 若代码与文档脱节，以实际源码与各目录 `README.md` 为准，并回补修订记录。
